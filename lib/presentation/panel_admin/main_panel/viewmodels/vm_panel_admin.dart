import 'package:flutter/material.dart';

class VmPanelAdmin extends StatelessWidget {
  const VmPanelAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
